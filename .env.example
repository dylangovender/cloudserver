# Edit and save as .env (DO NOT COMMIT .env)

PUID=1001
PGID=1001
TZ=Africa/Johannesburg

# Transmission RPC auth (set your own)
TR_USERNAME=admin
TR_PASSWORD=change-me-strong

# Domains (if/when you add Caddy later)
DOMAIN_CWA=cwa.dgdev.co.za
DOMAIN_READARR=readarr.dgdev.co.za

# Basic auth for reverse proxy (if/when you add Caddy)
BASIC_AUTH_USER=admin
BASIC_AUTH_HASH=\$2a\$14\$replace_with_bcrypt_hash

# SMTP for Kindle Sender (if/when you add it)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=no-reply@dgdev.co.za
SMTP_PASS=change-me
SMTP_FROM=no-reply@dgdev.co.za
